<%= form_for(@report_entry_detail) do |f| %>
  <% if @report_entry_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_entry_detail.errors.count, "error") %> prohibited this report_entry_detail from being saved:</h2>

      <ul>
      <% @report_entry_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_type_id %><br>
    <%= f.number_field :job_type_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :hours %><br>
    <%= f.number_field :hours %>
  </div>
  <div class="field">
    <%= f.label :comments %><br>
    <%= f.text_area :comments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
